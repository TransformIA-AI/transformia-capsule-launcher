# GIF Placeholder Plan

## Purpose

Atlas C02 requires the GIF placeholder to be documented. C02 documents the plan only.

## C02 status

No actual GIF is added in C02. No binary assets are added in C02. No screenshot is added in C02. No fake UI is added in C02.

## Future GIF should show

A future public-safe GIF may show:

1. Clone the public repository.
2. Run repository validation.
3. Select a public template later, after C03 adds public Capsule template manifests.
4. Configure local/BYOK later, after an Atlas-scoped BYOK scaffold exists.
5. View a TransformIA Cloud handoff later, after Cloud handoff docs are expanded.

## Future checklist

- Shows only public repository files.
- Shows no private runtime internals.
- Shows no customer data.
- Shows no secrets or API keys.
- Shows no connector credentials.
- Shows no fake execution.
- Shows no fake payment or Cloud signup.
- Uses truthful milestone labels.

## Naming convention for future assets

If a future approved PR adds visual assets, use a predictable public-safe path such as:

```text
assets/demo/transformia-capsule-launcher-v0-6-public-quickstart.gif
```

That future PR must explicitly approve binary assets and should update validators accordingly.

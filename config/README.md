# Config placeholder scaffold

This folder contains placeholder configuration only for the v0.6-C04 local/BYOK scaffold.

## Current status

- No active local launcher exists yet.
- No provider execution is configured.
- No private runtime dependency is introduced.
- No real keys, tokens or credentials belong here.
- `.env.example` at the repository root is documentation/sample material only.
- The local/BYOK path is planned for a future Atlas step, not enabled in C04.

## Files

- `local-byok.config.example.json` shows the future local/BYOK configuration shape with no-execution flags.
- `local-byok.config.schema.json` constrains the placeholder-only contract and prevents active provider/runtime claims.

Do not add real provider configuration, runtime adapters, connector settings or secrets to this folder.

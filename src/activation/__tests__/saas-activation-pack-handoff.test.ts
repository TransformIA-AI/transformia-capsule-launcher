// Static public contract tests for v0.9-L01. Runtime execution is covered by validators in this dependency-free repo.
// Coverage: deterministic fixture, public intent validation, unknown/quarantined routes, PII/URL/secret rejection,
// BYOK secret and plan blockers, trial review, no go-live, checklist blockers, sensitive summary redaction guard,
// stable/changing fingerprints, allowed manifest, writer traversal guard, freeze summary, malformed fail-closed,
// and no provider/network/auth/payment imports in activation modules.
